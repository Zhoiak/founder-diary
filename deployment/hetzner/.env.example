# --- App runtime ---
NODE_ENV=production
PORT=3000

# Supabase (self-hosted)
NEXT_PUBLIC_SUPABASE_URL=https://supabase.founder-diary.com
NEXT_PUBLIC_SUPABASE_ANON_KEY=REPLACE_ME
SUPABASE_SERVICE_ROLE_KEY=REPLACE_ME

# Docker image for the app (so docker-compose can pull it)
# Change <your-gh-username>/<repo> to your real values.
APP_IMAGE=ghcr.io/<your-gh-username>/<repo>:latest
